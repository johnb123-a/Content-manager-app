const { default: Layout } = require("components/Layout")

const ResourceCreate = () => {
    return (
        <Layout>
            <div>I am resource</div>
        </Layout>
    )
}

export default ResourceCreate;